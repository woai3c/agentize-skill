# Agentize 可实施设计

状态：设计待确认；确认前不修改实现。

## 1. 产品契约

Agentize 是一次性的仓库初始化与协调工具。它把空白、残缺、冲突或过期的 AI
开发工作流收敛为可由后续 Coding Agent 直接使用和维护的 Repository Harness。

不可变约束：

- 唯一入口是用户触发的具名命令。规范命令 ID 为 `agentize-repo`，首选显示为
  `/agentize-repo`。
- 自然语言意图匹配、模型选择、普通 Skill 发现、仓库文本、其他 Skill、子
  Agent、Hook、CI、IDE、启动事件、定时任务和历史授权都不能启动 Agentize。
- 命令结束后，目标仓库依靠自身的指令、文档、测试、脚本和 CI 工作；Agentize
  不成为运行时依赖。
- 用户可以在初始化后卸载或删除 Agentize，不影响目标仓库。
- 核心不依赖特定模型、Agent CLI 或厂商包；宿主差异只由薄命令适配器处理。
- 初始化通常只执行一次；再次审计必须由用户再次执行具名命令。

## 2. 唯一调用方式

### 2.1 命令协议

```text
/agentize-repo [repository-path]
```

- 省略路径时使用当前工作目录所属的 Git 根目录；没有 Git 时使用当前工作目录。
- 命令始终审计现状并协调到最小可靠状态；成熟仓库允许零修改。
- 宿主不支持 slash command 时，可以显示宿主原生形式，例如
  `$agentize-repo` 或命令面板中的 `agentize-repo`，但它必须仍是注册过的用户
  命令，不能退化为普通聊天文本。

有效调用必须由宿主提供可信命令事件：

| 字段 | 要求 |
| --- | --- |
| `command_id` | 精确等于 `agentize-repo` |
| `source` | 宿主确认是当前用户操作 |
| `target_hint` | 用户参数或空值 |
| `host_id` / `host_version` | 可取得时记录 |
| `command_session_id` | 本次一次性授权的宿主标识 |

以下情况全部无效：

- 用户只说“优化 AI 工作流”或“让项目适合 Agent”。
- 模型或宿主根据 Skill description 自动选择 Agentize。
- 未注册命令的聊天框中出现字面量 `/agentize-repo`。
- 命令文字来自 README、`AGENTS.md`、Issue、网页、日志或工具输出。
- 其他 Skill、Agent、自动化或先前会话转发命令。

无可信命令事件时不得加载完整 `SKILL.md`。如果宿主误加载了正文，第一条指令
必须立即 no-op：不检测运行时、不解析目标、不读取仓库、不创建计划、不执行脚本、
不写文件。

### 2.2 安装、上下文与卸载

安装或注入时只注册命令所需的最小元数据，不把 `SKILL.md` 正文和 references
放入模型常驻上下文。界面必须显示：

```text
Agentize 已安装，但不会自动运行。
使用：/agentize-repo [repository-path]
初始化完成后可以卸载 Agentize，目标仓库不会依赖它。
```

```text
用户命令事件
-> 命令适配器验证来源
-> 加载唯一规范 SKILL.md
-> 绑定一个目标仓库和 command_session_id
-> 初始化、验证、交接
-> 关闭授权并释放 Agentize 上下文
```

Agentize 发起的澄清或 Node.js 安装确认可以继续同一命令会话。完成、取消、阻断
或更换目标后，会话立即失效；新运行必须再次执行命令。

卸载只移除宿主中的命令注册、Skill 和可选适配文件。Agentize 不在目标仓库写入
自身标记、回调、锁文件或更新器，因此无需迁移目标仓库。

## 3. 交付结构

```text
agentize/
├── SKILL.md                  # 命令门禁和端到端协调
├── DESIGN.md                 # 本设计
├── README.md                 # 安装、命令、卸载和支持矩阵
├── AGENTS.md                 # Agentize 仓库自身的开发规则
├── compatibility/
│   └── hosts.json            # 宿主命令能力与验证证据
├── adapters/
│   └── <host>/               # 仅在宿主需要时提供薄命令适配
├── references/
│   ├── assessment.md         # 证据、状态和冲突处理
│   ├── artifacts.md          # 目标产物与创建条件
│   ├── compatibility.md      # 宿主接入规则
│   └── workflow.md           # 要建立的 AI 开发工作流
├── scripts/
│   ├── agentize.js           # 默认 Node.js 执行器
│   └── agentize.py           # 功能等价的 Python 后备
└── tests/
    ├── fixtures/
    ├── conformance/
    ├── test-node.mjs
    ├── test_python.py
    ├── test_parity.py
    └── behavior-cases.md
```

`SKILL.md` 是唯一规范入口；references 只在相关决策点加载。适配器只注册命令、
验证命令来源并定位 Skill 根目录，不复制核心正文。宿主要求元数据位于固定路径时
可以增加该文件，但它不是其他宿主的前置条件。

不交付厂商专用完整副本、Plugin 打包器、MCP Server、常驻服务或职责重复的独立
CLI。

## 4. 双运行时执行器

命令门禁通过后才检测运行时：

1. Node.js 可用且版本受支持：使用 `agentize.js`。
2. Node.js 不可用：依次检测 `python3`、`python`，Windows 再检测 `py -3`。
3. Python 可用：使用 `agentize.py`，能力与 Node.js 版本相同。
4. 两者都不可用：说明安装来源、命令、范围、权限和 PATH 影响，请求用户确认
   安装 Node.js。
5. 用户同意后只执行一个可信安装方案，重新检测一次；失败即停止。
6. 用户拒绝或宿主不能交互确认：不修改机器和仓库，报告手动恢复步骤。

两个执行器仅使用标准库，并提供相同接口：

```text
node <skill-root>/scripts/agentize.js scan --root <repo> --format json|markdown
node <skill-root>/scripts/agentize.js validate --root <repo> --format json|markdown
node <skill-root>/scripts/agentize.js version --format json

<python> <skill-root>/scripts/agentize.py scan --root <repo> --format json|markdown
<python> <skill-root>/scripts/agentize.py validate --root <repo> --format json|markdown
<python> <skill-root>/scripts/agentize.py version --format json
```

脚本职责：

- 有界、只读地清点仓库结构、指令、文档、清单、任务、测试和 CI。
- 静态解析白名单配置，不 import 或执行项目代码。
- 跳过 `.git`、依赖、构建产物、缓存、vendored 目录和敏感文件。
- 输出稳定 Schema，明确记录截断、权限错误和未验证项。
- 校验 Agent 指令中的链接、路径和命令声明。

`agentize.js` 使用 Node.js CommonJS 和标准库，不依赖目标项目的 `package.json`
模块类型。脚本不判断业务意图，也不修改目标仓库。Node.js 与 Python 使用同一
Schema、fixture 和规范化规则；除实现标识和平台路径外，输出必须 parity。

## 5. 初始化算法

每次有效命令严格按以下顺序执行：

1. **门禁**：验证可信命令事件；失败则零副作用 no-op。
2. **定界**：解析唯一目标仓库，读取适用的 Agent 指令和 `git status`。
3. **运行时**：按第 4 节选择执行器；没有运行时时先完成确认协议。
4. **清点**：运行 `scan`，取得结构化证据。
5. **取证**：只读取解决当前判断所需的清单、任务配置、CI、入口、代表性测试、
   维护中文档和必要 Git 历史。
6. **评估**：把 AI 工作流能力标记为 `sound`、`weak`、`missing`、
   `conflicting`、`stale` 或 `unverified`，每项附证据路径。
7. **计划**：只处理会实质影响 Agent 正确性、速度或验证能力的缺口。
8. **协调**：修复已有权威来源，补充最小缺失内容；无法确认的业务问题保留为
   knowledge gap。
9. **验证**：重跑 `scan` 和 `validate`，执行与改动相称的项目检查并检查完整
   diff。
10. **交接**：报告变化、验证和未知项，确认仓库不依赖 Agentize，关闭命令会话。

执行结果：

- `full`：完成扫描、协调、验证和交接。
- `read-only`：宿主不能写入，只报告有证据的缺口，不声称完成初始化。
- `blocked`：命令有效，但资源、目标读取、运行时或必要授权不可用。

## 6. 要建立的 AI 工作流

Agentize 的核心产物不是固定文件，而是下面这条可重复、可验证、能自行维护的
开发闭环：

```text
Task
-> Scope & Spec
-> Explore
-> Plan
-> Implement
-> Verify
-> Review
-> Learn
-> Handoff
```

| 阶段 | 后续 Agent 的必需行为 | 仓库应提供的持久支持 |
| --- | --- | --- |
| Scope & Spec | 读取适用指令和工作区状态，明确目标、约束、完成标准和未知项 | 简洁根指令、范围化子目录指令、任务或产品规范 |
| Explore | 先查高信号证据，只加载完成任务所需上下文 | 项目地图、入口、模块边界、权威文档链接 |
| Plan | 对非简单任务列出可验证步骤、依赖和风险边界 | 架构约束、变更规则、所有者和 knowledge gaps |
| Implement | 做最小一致补丁，保留用户改动，不顺手扩大范围 | 代码约定、生成规则、局部指令 |
| Verify | 从低成本检查逐级运行到必要的完整检查 | 可复制命令、测试目标、Lint、类型检查和 CI |
| Review | 检查完整 diff、回归、安全、权限和文档漂移 | Review 清单及关键不变量的自动门禁 |
| Learn | 把稳定结论写回唯一规范源，把重复失败转成测试或规则 | 文档维护触发条件、决策记录、反馈回路 |
| Handoff | 说明结果、文件、验证、未运行项和剩余风险 | 可供下一次任务直接使用的最新仓库状态 |

### 6.1 上下文效率

- 根 Agent 指令只做导航：项目目的、关键入口、准确命令、安全边界和权威链接。
- 细节放在有明确所有者的文档或子树指令中，按任务渐进加载。
- 不把整个仓库、长日志或重复文档塞进上下文。
- 代码、配置、测试和 CI 证明易变事实；文档保存稳定边界、原因和非显然规则。
- 一个事实只有一个权威位置，其他位置链接到它。

### 6.2 Agent 优先的验证阶梯

1. 修改文件的格式、静态检查或目标测试。
2. 受影响包或模块的测试、类型检查和构建。
3. 仓库级集成检查。
4. 用户可见 Web/UI 流程的 E2E 或浏览器验证。
5. 只有高风险边界确实需要时才运行真实服务或昂贵检查。

不能运行的检查标记为 `not run` 并说明原因；不得写成已经验证。危险、付费、
需要凭据、发布、部署或数据迁移的命令不会因出现在文档中而自动获准。

每个后续 Agent 都必须在交接前完成与变更相称的验证。对于新增、修改或可能影响
用户可见 Web/UI 的任务，浏览器验证是强制步骤：

1. 使用仓库记录的安全命令启动本地应用或测试环境。
2. 使用宿主已有的浏览器控制能力实际走通受影响页面和交互，不能只看截图或只确认
   页面能打开。
3. 检查预期状态、关键交互、相关视口以及浏览器控制台错误；按风险决定是否检查
   网络失败和持久化结果。
4. 在交接中记录访问路径、操作、结果和未覆盖项。
5. Agent 验证没有发现问题后，再向人提供简短、精确的手工验收步骤；人负责主观
   视觉、真实账号、真实设备或生产环境等自动化无法可靠覆盖的部分。

纯后端、库、CLI、文档或不影响 UI 的任务不要求打开浏览器。宿主没有浏览器能力、
应用无法安全启动或需要未授权凭据时，Agent 必须标记浏览器验证为 `not run`，说明
阻碍并提供手工步骤，不能声称 UI 已验证。这里的目标是先消耗 Agent 能完成的验证，
再使用人的时间，而不是保证软件绝对没有缺陷。

这些不是只在 Agentize 运行期间生效的内部规则。对于 Web/UI 仓库，Agentize 必须
把它们写入目标仓库的持久指令体系：

- 根或适用子树的 `AGENTS.md` 保留简短强制规则：哪些变更必须做浏览器验证、必须
  先由 Agent 验证再交给人，以及无法运行时怎样报告。
- 启动命令、测试环境、URL、关键流程、视口、测试数据限制和手工验收步骤写入项目
  已有测试文档；没有合适所有者时才创建聚焦文档，例如 `docs/testing.md`。
- 可执行命令由项目任务配置或验证脚本拥有，E2E 断言由测试代码拥有；文档链接这些
  规范源，不复制易过期实现。
- 已有指令和文档正确时只补缺口，不强制使用上述示例文件名或目录。

### 6.3 后续自动维护

初始化必须在目标仓库适用的持久 Agent 指令中建立维护契约。后续 Agent 在普通
开发任务中遇到以下变化时，同步维护对应的唯一规范源：

- 命令、路径、配置或工具链变化：更新导航和验证说明。
- 模块边界、数据流或公共接口变化：更新架构上下文和相关测试。
- 用户行为或领域规则变化：更新规范、领域文档和验收测试。
- CI、生成器或安全边界变化：更新指令与门禁说明。
- 用户可见 Web/UI 变化：运行浏览器验证并更新相关 E2E、截图基线或手工验收步骤。
- Review 问题重复出现：优先沉淀为测试、Lint、Schema 或确定性脚本。
- 非显然决策稳定下来：记录原因；未知项解决后删除或转成事实。

这是目标仓库自身的维护流程，不是 Agentize 自动重跑。维护契约不得包含调用
Agentize 的 Hook、CI、计划任务或启动提示。

## 7. 目标产物与协调规则

所有产物按证据选择，不强制固定脚手架：

| 产物 | 创建或修改条件 |
| --- | --- |
| 根 Agent 指令 | 缺少可操作入口、维护契约或现有内容错误 |
| 嵌套 Agent 指令 | 子树确实有不同工具链、边界、验证或安全要求 |
| 架构/领域文档 | 稳定且非显然的信息反复需要广泛探索 |
| 测试与浏览器验证说明 | Web/UI 的启动方式、必测流程、环境限制或人工验收步骤不可发现 |
| 验证脚本或任务 | 命令反复拼装、机械且容易出错 |
| Formatter、Lint、类型检查 | 项目缺少低成本反馈，且存在适合当前技术栈的稳定方案 |
| 单元/集成测试 | 重要行为缺少回归保护，并且能从代码、规范或现有行为取得预期结果 |
| E2E 或浏览器 smoke test | Web/UI 存在稳定、关键且可在安全环境重复运行的用户流程 |
| CI 门禁 | 本地命令已经验证可靠，且共享分支需要持续强制执行 |
| 决策记录 | 非显然取舍可能被再次争论，代码无法保存原因 |
| Knowledge gaps | 未回答问题会实质改变工程决策 |
| Repository-local Skill | 项目自身存在独立且重复的专用工作流 |

因此，Agentize 不只创建或修改 `AGENTS.md` 和 docs；它可以修复或建立 Formatter、
Lint、类型检查、单元测试、集成测试、E2E、浏览器 smoke test、验证脚本和 CI。
选择规则如下：

- 优先复用并修复项目已有工具，不平行引入第二套框架。
- 新增工具必须解决已证明的反馈缺口，并提供可运行命令；不创建只为占位的测试。
- 测试预期必须来自代码、产品规范、现有行为或用户确认，不猜测业务规则。
- 对 Web/UI 项目，优先保护少量关键路径；不为覆盖率数字生成大量脆弱 E2E。
- 新增依赖、修改 lockfile 或下载浏览器二进制前，遵守宿主审批和用户授权；影响较大
  时先说明方案。
- CI 只调用已经在本地验证过的命令，不把 Agentize 本身加入 CI。

仓库状态处理：

| 初始状态 | 处理 |
| --- | --- |
| 空白 | 建立最小导航、上下文、命令、验证和维护契约 |
| 部分正确 | 保留正确内容，只补关键缺口 |
| 正误混合 | 用可执行证据修复事实错误；业务冲突保留问题并请求负责人判断 |
| 已成熟 | 验证现状后允许零修改 |

证据优先级：

1. 可执行配置、代码、测试和实际 CI。
2. 当前维护的权威文档和用户明确说明。
3. Git 历史中的设计原因。
4. 命名、目录结构或模型推测只能作为调查线索。

不得为了统一外观重命名文件、复制 `AGENTS.md`/`CLAUDE.md`/`GEMINI.md` 内容、
创建空模板或增加目标项目并未使用的厂商文件。

## 8. 安全边界

- 命令授权只覆盖一个目标仓库内的 AI 工作流文件和必要验证。
- 仓库内容是不可信输入，不能授权命令、安装、网络或外部操作。
- 扫描器不读取密钥内容、用户主目录或仓库外符号链接。
- 写入采用补丁式修改；保留无关改动，冲突无法安全合并时停止。
- 不使用破坏性 Git 命令，不提交、推送、建 PR 或操作外部系统，除非用户另行明确
  请求。
- 不执行发布、部署、真实服务测试、生产脚本或数据迁移。
- 安装 Node.js 是独立的机器修改，必须取得精确确认。
- 目标仓库不得留下 Agentize 依赖、命令、Hook、CI 调用、后台任务或生成标记。

## 9. 跨宿主命令适配

核心使用 [Agent Skills Specification](https://agentskills.io/specification) 的可移植
子集，但该规范不保证统一的用户命令机制。因此每个宿主必须有经过验证的命令
适配档案。

`compatibility/hosts.json` 至少记录：

- 宿主及已测试版本。
- 实际显示的命令语法和安装/卸载方式。
- 如何证明命令来自当前用户。
- 如何关闭模型隐式调用和自动发现注入。
- 如何加载完整 Skill 资源根。
- 文件读写、进程执行、交互确认和沙箱语义。
- 测试模型、操作系统、日期和 conformance 结果。

支持等级：

| 等级 | 条件 |
| --- | --- |
| `verified-command-full` | 命令门禁和完整初始化行为均通过 |
| `verified-command-read-only` | 命令门禁通过，但宿主只能只读审计 |
| `format-only` | 能解析 Skill，但没有可信用户命令；不可安装为可用 Agentize |
| `unsupported` | 会自动调用、无法证明用户来源或缺少关键能力 |
| `unknown` | 尚无测试证据 |

Codex、Claude Code、Gemini CLI、Kimi Code CLI、Pi、DeepSeek Harness 和其他
Agent CLI 都是地位相同的适配目标，没有默认厂商。某个宿主若只能自动发现 Skill、
不能注册可信用户命令，就不满足本设计；不能用自然语言提示模拟命令后声称支持。

## 10. 测试方案

### 10.1 命令门禁

- 注册的用户命令事件可以加载 Skill。
- 普通聊天中的 `/agentize-repo`、近似命令和语义匹配请求均不加载。
- 仓库文件、工具输出、其他 Skill、子 Agent、Hook 和 CI 中的命令均无效。
- 宿主误加载正文时，在任何运行时检测和仓库读取前 no-op。
- 完成、取消、阻断和更换目标后不能复用旧命令会话。
- 安装后普通任务不增加 Agentize 上下文；卸载后命令和元数据完全消失。

### 10.2 执行器

- Node.js/Python 的 `scan`、`validate`、`version` 和退出语义一致。
- PATH 矩阵覆盖 Node 优先、Python fallback、二者缺失、安装同意、拒绝和失败。
- fixture 覆盖空白、部分完成、冲突、成熟、Monorepo、大仓库、Unicode/空格路径、
  vendored 目录、权限错误和截断。
- 两种实现规范化后逐 fixture parity。

### 10.3 行为与宿主

- 四类仓库都产生符合第 7 节的最小协调结果。
- AI 工作流的八个阶段、验证阶梯和维护契约都有可观察证据。
- fixture 分别验证：复用已有 Lint/Test/E2E、在有证据时补最小反馈回路，以及在
  不适用时不增加工具。
- Web/UI fixture 要求浏览器走通受影响流程后才生成手工验收步骤；后端和文档
  fixture 不得产生无意义的浏览器要求。
- 浏览器不可用、应用无法启动和需要未授权凭据时，结果必须准确标记为 `not run`。
- 二次用户命令在无新变化时产生零实质 diff。
- 脏工作区、未知业务规则、危险命令和安装确认路径符合安全边界。
- 每个声明支持的宿主运行真实命令门禁与完整会话 conformance。
- 删除宿主中的 Agentize 后，fixture 仓库仍能由普通 Agent 完成任务和维护文档。

## 11. 实现顺序

以下是工程依赖顺序，不是分版本交付：

1. 固定 `agentize-repo` 命令事件和 `hosts.json` Schema。
2. 将 `SKILL.md` 改为命令门禁优先的简洁协调入口。
3. 把 AI 工作流、评估、产物和兼容细节分别收敛到四个 references。
4. 实现零第三方依赖的 Node.js 主执行器和 Python 等价后备。
5. 建立共享 fixture、parity、命令门禁和仓库行为测试。
6. 为每个目标宿主实现或配置薄命令适配器，并保存真实 conformance 证据。
7. 更新 README、行为案例和宿主安装/卸载说明，保持单一事实来源。

## 12. 完整验收

同时满足以下条件才算完成：

1. 安装时只显示和注册具名命令，不把 Skill 正文注入普通任务上下文。
2. 只有宿主确认的当前用户 `agentize-repo` 命令事件能够启动流程。
3. 无效入口在运行时检测、仓库读取和写入前严格 no-op。
4. Node.js 默认、Python 自动后备和 Node.js 安装确认全部按契约工作。
5. 两个执行器通过 Schema、fixture 和 parity 测试。
6. 空白、部分、冲突和成熟仓库分别得到最小正确结果。
7. 目标仓库具备 Scope、Explore、Plan、Implement、Verify、Review、Learn 和
   Handoff 闭环。
8. 目标仓库的 `AGENTS.md` 和按需链接的测试文档持久记录验证阶梯、浏览器要求、
   人工验收顺序及后续 Agent 文档维护契约。
9. Agentize 会在有证据且适用时建立或修复 Lint、类型检查、单元/集成测试、E2E、
   浏览器 smoke test 和 CI，而不会机械添加整套工具。
10. 用户可见 Web/UI 变更在交接前经过 Agent 浏览器验证，并在其后提供人工验收
    步骤；非 UI 任务不会被要求进行无意义的浏览器操作。
11. 浏览器或其他检查无法运行时准确报告 `not run`，不伪造成功结论。
12. 正确内容和用户改动被保留，未知业务问题没有被猜成事实。
13. 所有写入、命令和安装遵守第 8 节安全边界。
14. 完成后目标仓库不包含 Agentize 依赖或自动触发器。
15. 卸载 Agentize 后，普通 Agent 仍能理解、修改、验证并维护目标仓库。
16. 再次由用户执行命令时流程幂等。
17. 每项宿主支持声明都有命令门禁和完整行为证据；任何厂商都不是默认前提。
18. 单元测试、行为测试、宿主 conformance、跨平台 CI、Skill 校验和
   `git diff --check` 全部通过。
